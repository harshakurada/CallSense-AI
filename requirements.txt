# --- Core / config ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
pyyaml==6.0.2

# --- API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
httpx==0.27.2

# --- Dashboard ---
streamlit==1.39.0
requests==2.32.3

# --- Database ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.10

# --- Experiment tracking ---
mlflow==2.16.2

# --- Deep learning / speech / NLP (installed starting Module 2, when the
# audio+ASR pipeline is actually built — declared now so the eventual stack
# is explicit) ---
torch==2.4.1
transformers==4.45.1
faster-whisper==1.0.3
librosa==0.10.2
soundfile==0.12.1
pyannote.audio==3.3.2
sentence-transformers==3.1.1

# --- Testing ---
pytest==8.3.3
