# --- Core / config ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
pyyaml==6.0.2

# --- API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
httpx==0.27.2

# --- Dashboard ---
streamlit==1.39.0
requests==2.32.3

# --- Database ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.10

# --- Experiment tracking ---
mlflow==2.16.2

# --- Audio + ASR (Module 2) ---
# faster-whisper runs on ctranslate2, not torch, so no torch dependency here.
# av is left unpinned by faster-whisper's own spec (<13,>=11) because that
# range has no Python 3.13 wheel on Windows; 13.1.0 is used instead and works
# fine for the decoding faster-whisper needs (see docs/DATASETS.md notes).
faster-whisper==1.0.3
av==13.1.0
ctranslate2==4.8.2
onnxruntime==1.30.0
tokenizers==0.20.3  # downgraded from 0.23.2 by transformers' <0.21 pin (Module 4) — faster-whisper still passes all tests on it
huggingface_hub==1.31.0
librosa==0.10.2
soundfile==0.12.1
jiwer==3.0.4
matplotlib==3.9.2
pyarrow==25.0.1  # scripts/download_data.py reads the librispeech-dummy parquet file directly

# --- Speaker diarization (Module 3) ---
# pyannote.audio's pretrained pipeline is gated (requires an accepted-terms
# HF token this project doesn't have) — see docs/DIARIZATION.md. Diarization
# instead uses an ungated speechbrain embedding + sklearn clustering
# pipeline. torch/torchaudio are CPU wheels from
# https://download.pytorch.org/whl/cpu (PyPI's default torch wheel bundles
# CUDA and is far larger than needed here).
# pip install --index-url https://download.pytorch.org/whl/cpu torch==2.7.1 torchaudio==2.7.1
torch==2.7.1
torchaudio==2.7.1
speechbrain==1.1.1
scikit-learn==1.9.1
pyannote.core==6.0.1     # DER computation only (src/diarization/evaluation.py) — not the gated pipeline
pyannote.metrics==3.2.1

# --- NLP: intent, sentiment, emotion (Module 4) ---
transformers==4.45.1
accelerate==1.0.1
datasets==5.0.1     # loads the intent/sentiment/emotion datasets from the HF Hub

# --- NER + PII (Module 5) ---
spacy==3.8.16
seqeval==1.2.2      # entity-level P/R/F1 (src/nlp/ner/*)
Faker==30.8.2        # synthetic business-entity training data — see docs/NER.md
# after installing requirements.txt, also run:
#   python -m spacy download en_core_web_sm

# --- NLP (Module 8+, not installed yet) ---
sentence-transformers==3.1.1

# --- Testing ---
pytest==8.3.3
